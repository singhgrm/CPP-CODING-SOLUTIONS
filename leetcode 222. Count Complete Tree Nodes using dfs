//apply dfs if there is null means no node 
//else for node 1+left +right


int countNodes(TreeNode* root) {
        if(root==NULL)
        {
            return 0;
        }
    return 1+countNodes(root->left)+countNodes(root->right);
    }
